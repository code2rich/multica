# -----------------------------------------------------------------------------
# Agent runtime storage (required for every formal task)
# -----------------------------------------------------------------------------
AGENT_WORK_DIR=/absolute/path/to/agentwaker/research-operator/workdir
AGENT_MEMORY_FILE=/absolute/path/to/agentwaker/research-operator/agent-soul/MEMORY.md

# -----------------------------------------------------------------------------
# Platform API credentials (required for trend research)
# -----------------------------------------------------------------------------
PLATFORM_API_KEY=
PLATFORM_API_BASE=https://api.example.com
