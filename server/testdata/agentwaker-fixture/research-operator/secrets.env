# This is an UNRELATED .env-style file outside the recognized {role}/env/.env
# path. The scanner must NOT read it, and its value must never appear in any
# preview, snapshot, plan, log, or API response.
UNRELATED_LEAK_CANARY=unrelated-secret-must-not-be-read
