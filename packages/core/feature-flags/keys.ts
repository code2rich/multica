export const COMPOSIO_MCP_APPS_FLAG = "composio_mcp_apps";

/**
 * Gates the AgentWaker directory integration: source configuration, the daemon
 * scan protocol, sanitized snapshot previews (M1), atomic import (M2), runtime
 * capability injection (M3), and continuous resync (M4). Ships dark.
 */
export const AGENTWAKER_DIRECTORY_SYNC_FLAG = "agentwaker_directory_sync";
